import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	controlBarContainer: {
		alignItems: "center",
		bottom: 30,
		flex: 1
	},
	safe: {
		backgroundColor: "#0f0",
	}
})